import tw from "twin.macro";

const Container = tw.div`h-screen flex items-center justify-center`;
const Title = tw.h1`font-bold text-3xl text-textBlack`;

export default function Products() {
  return (
    <Container>
      <Title>ini halaman products belum selesai kak 😄</Title>
    </Container>
  );
}
